@* @using Microsoft.AspNet.Identity *@





@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
          @*@Html.ActionLink("Hello " + GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" }) *@      
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            <ul class="nav navbar-nav">
                <li class="dropdown close-modal">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" title="Register">
                        <i class="fa fa-tasks"></i> Register
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li class=" dropdown dropdown-submenu">
                            @Html.ActionLink("Employers", "Register", "Account", null, new { @class = "dropdown-toggle" })
                            @Html.ActionLink("Job Seekers", "RegisterJS", "Account", null, new { @class = "dropdown-toggle" })

                        </li>
                    </ul>
                </li>
            </ul>
        </li>
        <li>
            <ul class="nav navbar-nav">
                <li class="dropdown close-modal">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" title="Login">
                        <i class="fa fa-tasks"></i> Login
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li class=" dropdown dropdown-submenu">
                            @Html.ActionLink("Employers", "ELogin", "Account", null, new { @class = "dropdown-toggle" })
                            @Html.ActionLink("Job Seekers", "Login", "Account", null, new { @class = "dropdown-toggle" })
                        </li>
                    </ul>
                </li>
            </ul>

        </li>
    </ul>
}

